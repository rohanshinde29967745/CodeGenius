import { query } from './config/db.js'; async function run() { try { await query('CREATE TABLE contest_submissions (id SERIAL PRIMARY KEY, contest_id INT, problem_id INT, user_id INT, code TEXT, language VARCHAR(50), passed_tests INT, total_tests INT, score INT, is_accepted BOOLEAN, submitted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)'); console.log('Table created!'); process.exit(0); } catch(e) { console.error(e); process.exit(1); } } run();
